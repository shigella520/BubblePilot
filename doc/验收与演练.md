# BubblePilot 验收与演练

本文固定 Web 异步交互和可靠性演练的有限范围。产品功能是否存在以[产品与范围](产品与范围.md)为准；HTTP 和工作流字段以机器契约为准。

## 严重级别

| 级别 | 判定 | 发布规则 |
| --- | --- | --- |
| P0 | 认证绕过、敏感数据泄漏、重复外部回复、破坏持久化幂等、错误处置未知发送 | 必须立即修复，阻止发布 |
| P1 | 重复提交写操作、显示错误状态、成功后保留失效入口、失败后错误改写本地状态 | 发布候选必须修复 |
| P2 | 不改变业务结果的加载提示、视觉或可访问性增强 | 记录但不阻塞 |

新增问题只有能映射到产品需求、接口契约或下方通用合同时，才能提升为 P0/P1。不要在一次修复中无限扩大 UI 审查范围。

## Web 通用合同

所有 P0/P1 写操作必须满足：

1. 认证、二次验证、版本检查、审计和幂等由服务端执行；前端禁用按钮不是安全边界。
2. 请求开始前清除旧反馈，只锁定会冲突的目标操作。
3. 同一目标的重复点击不能产生并发写请求。
4. 成功优先用接口返回值更新目标；刷新失败不能掩盖已经成功的写入。
5. 业务失败、网络失败、`401` 和 `409` 保留可解释结果，不能错误改写原状态。
6. 取消确认不发请求、不改变状态。
7. 会话或二次授权到期时，正文、搜索结果、导出预览和敏感操作状态立即清理。

读取操作必须避免旧响应覆盖新选择，失败后可恢复，并且不泄露敏感数据。

## 固定 Web 交互矩阵

`apps/web/src` 的用户可观察异步行为固定为 35 条路径。纯同步表单编辑、展开收起和本地排序不单独计数。

### 认证与概览（4）

| ID | 路径 | 级别 | 验收重点 |
| --- | --- | --- | --- |
| `WEB-AUTH-001` | 登录 | P0 | 提交防重复，失败保留登录页 |
| `WEB-AUTH-002` | 敏感操作验证 | P0 | 绑定会话，使用服务端绝对到期时间 |
| `WEB-AUTH-003` | 退出登录 | P1 | 失效会话幂等退出，其他失败可重试 |
| `WEB-DASH-001` | 概览刷新 | P2 | 失败可见，不泄露正文 |

### 消息、搜索与导出（8）

| ID | 路径 | 级别 | 验收重点 |
| --- | --- | --- | --- |
| `WEB-MSG-001` | 聊天列表刷新 | P2 | 读取失败可恢复 |
| `WEB-MSG-002` | 聊天监听启停 | P1 | 只锁定目标，失败保留状态 |
| `WEB-MSG-003` | 敏感消息搜索 | P0 | 授权到期立即清理结果 |
| `WEB-EXP-001` | 导出任务加载 | P2 | 所有者隔离，失败可见 |
| `WEB-EXP-002` | 冻结导出预览 | P1 | 防重复，失败不生成本地预览 |
| `WEB-EXP-003` | 确认导出 | P1 | 使用冻结数量和时间，成功不被刷新失败掩盖 |
| `WEB-EXP-004` | 取消导出 | P1 | 取消确认零请求，只锁定目标任务 |
| `WEB-EXP-005` | 下载导出 | P1 | 成功和失败反馈明确，不缓存正文 |

### 工作流与触发器（9）

| ID | 路径 | 级别 | 验收重点 |
| --- | --- | --- | --- |
| `WEB-FLOW-001` | 列表刷新 | P2 | 读取失败可恢复 |
| `WEB-FLOW-002` | 切换工作流和版本 | P1 | 旧响应不覆盖当前选择 |
| `WEB-FLOW-003` | 创建工作流 | P1 | 表单锁定，失败保留画布 |
| `WEB-FLOW-004` | 保存候选版本 | P1 | 使用接口版本更新，失败保留编辑 |
| `WEB-FLOW-005` | 发布版本 | P1 | 防重复，失败不改变发布状态 |
| `WEB-FLOW-006` | 工作流启停 | P1 | 只锁定目标，成功原位更新 |
| `WEB-TRIGGER-001` | 条件预览 | P2 | 无 AI、回复或持久化副作用 |
| `WEB-TRIGGER-002` | 创建触发器 | P1 | 默认停用，失败保留条件 |
| `WEB-TRIGGER-003` | 触发器启停 | P1 | 保留冲突信息和失败状态 |

### AI Provider 与路由（10）

| ID | 路径 | 级别 | 验收重点 |
| --- | --- | --- | --- |
| `WEB-AI-001` | Provider 与路由刷新 | P2 | 失败可见 |
| `WEB-AI-002` | 创建或更新 Provider | P1 | Secret 不回显，提交防重复 |
| `WEB-AI-003` | Provider 启停 | P1 | 只锁定目标，失败保留状态 |
| `WEB-AI-004` | 连接与能力测试 | P2 | 三类结果独立，不改写生产状态 |
| `WEB-AI-005` | 健康状态重置 | P1 | 成功原位更新，失败不清空旧状态 |
| `WEB-AI-006` | Provider 删除 | P1 | 取消零请求，冲突时保留原行 |
| `WEB-AI-007` | Provider 排序 | P1 | 拖拽和按钮共用写入锁 |
| `WEB-AI-008` | 创建或更新路由 | P1 | 候选与版本原子更新 |
| `WEB-AI-009` | 路由启停 | P1 | 只锁定目标，失败保留状态 |
| `WEB-AI-010` | 路由删除 | P1 | 取消零请求，冲突时保留原行 |

### 执行与人工恢复（5）

| ID | 路径 | 级别 | 验收重点 |
| --- | --- | --- | --- |
| `WEB-EXEC-001` | 列表和恢复队列筛选 | P2 | 读取失败可恢复 |
| `WEB-EXEC-002` | 执行详情 | P1 | 只让最后一次选择更新详情；路由版本、候选过滤、Fallback 和图片降级与执行时记录一致，历史缺失不反推 |
| `WEB-EXEC-003` | 人工重试 | P0 | 确认、防重复、未知发送阻断、关联新执行 |
| `WEB-EXEC-004` | 人工关闭 | P0 | 确认、防重复、等待执行保护、保留轨迹 |
| `WEB-EXEC-005` | 恢复队列批量关闭 | P0 | 全部分页、敏感验证、确认、防重复、原子状态过滤、保留 unknown 轨迹、实际关闭数量及刷新失败提示 |

当前基线没有未处理 P0 或 P1。新变更必须重新验证受影响路径，不能只沿用历史结论。

## Web 停止条件

一次 Web 发布审查在以下条件同时满足时结束：

1. 受影响路径没有未处理 P0/P1；
2. 针对性测试、Web 类型检查、生产构建和仓库质量门禁通过；
3. 受影响页面完成桌面与 390px 移动视口验收，无意外横向溢出；
4. P2 单独记录，不继续扩大当前变更范围。

## 死信与恢复演练

自动化基线：

```bash
pnpm vitest run tests/workflow-application.test.ts tests/reliability-guards.test.ts
```

目标环境步骤：

1. 用虚构测试聊天触发一个明确失败、且没有未知出站的工作流。
2. 在恢复队列记录源执行 ID、状态和错误码。
3. 在正常等待期尝试人工重试，应返回 `EXECUTION_RETRY_STILL_ACTIVE`。
4. 超过 `STALE_RETRY_SECONDS` 后重试，确认新执行通过 `retryOfExecutionId` 关联源执行，且最多产生一次预期回复。
5. 制造回复结果未知场景，确认人工重试被拒绝；核对 BlueBubbles 后人工关闭。
6. 检查 `execution.retry` 和 `execution.close` 审计只包含 ID、结果和关联 ID。

通过标准：重复点击不创建第二个直接恢复子执行；旧等待执行器不能覆盖人工关闭或逾期接管；`unknown` 禁止重发；限流、并发和队列上限返回稳定错误。

## 备份恢复演练

```bash
BACKUP_PATH=$(scripts/backup-postgres.sh)
scripts/verify-postgres-backup.sh "$BACKUP_PATH"
```

通过标准：

- `.dump` 非空且 SHA-256 匹配；
- custom-format 备份可恢复到脚本创建的随机临时数据库；
- `schema_migrations` 和消息、执行、Provider、会话、导出关键表可查询；
- 临时数据库在成功或失败后都被删除；
- 生产数据库名称、大小和连接状态未改变；
- 备份复制到受控异机位置，演练记录不包含数据内容。

## 升级与回滚演练

1. 当前和目标镜像都固定为 `X.Y.Z` 或 `sha-<commit>`，不用 `dev` 或 `latest` 作为回滚锚点。
2. 创建备份并运行：

```bash
scripts/preflight-upgrade.sh "$BACKUP_PATH"
```

3. 启动目标镜像：

```bash
docker compose pull app
docker compose up -d --no-build
scripts/diagnose.sh
```

4. 验证登录、二次授权、聊天监听、虚构测试消息、工作流回复、AI Fallback、执行详情、搜索（若启用）和导出预览。
5. 应用回滚时切回上一精确镜像。数据库不反向迁移；若旧应用不兼容新 Schema，恢复演练备份和对应旧应用版本。

## 演练记录

每次正式发布前追加一组实际记录。状态只使用 `通过`、`失败`、`待执行`；单元测试或 Shell 语法检查不能替代真实 PostgreSQL 恢复与镜像回滚。

| 日期 | 环境 | Commit / 镜像 | 演练 | 耗时 | 结果 | 脱敏证据 / 后续问题 |
| --- | --- | --- | --- | --- | --- | --- |
| 待填写 | 目标环境 | `X.Y.Z` 或 `sha-...` | 死信恢复 | — | 待执行 | — |
| 待填写 | 目标环境 | `X.Y.Z` 或 `sha-...` | 备份恢复 | — | 待执行 | — |
| 待填写 | 目标环境 | `X.Y.Z` 或 `sha-...` | 升级与回滚 | — | 待执行 | — |

记录日期、环境、精确镜像或 Commit、最高迁移版本、操作者、耗时、结果和后续问题。不得粘贴 Secret、真实消息、完整 Prompt、数据库转储内容或未脱敏日志。

## 长期聊天检索验收记录（2026-09-10，开发中）

- 本地 `pnpm verify` 通过 309 项测试（无跳过），生产依赖安全检查、ShellCheck、Markdown 链接和前后端构建通过；全新数据库迁移及重复向量准备通过。低权限账号扩展准备失败时安全返回已实测。
- 已在独立本地 PostgreSQL 16.15 + pgvector 0.8.0 执行普通迁移及向量准备，使用虚构数据测试索引、来源与清理。
- 已增加 Ollama/兼容协议输出校验、中文分词、长消息切片、Agent 独立历史工具、必须联网隔离、无效引用修正和敏感 API 权限回归。
- SQL 合成基准：1024 维、单聊天、1 万行热查询 p95 为 31 ms，10 万行为 272 ms；数据库占用分别约 56.5 MB、565.6 MB。结果仅覆盖精确距离查询，不包含 Embedding、完整检索、Worker 吞吐或 Mac mini 负载。
- 真实模型评测暂缓：用户尚未部署 Embedding 服务。四十个虚构问题及十个保留验证题已纳入仓库；真实 Recall@5、摘要基线对比、无答案支持性、Agent 调用选择及目标 Mac mini 并行负载均待执行，不计为已通过。
- Docker 旧镜像备份恢复、新镜像扩展准备及完整镜像构建由 CI 演练；未取得 CI 结果前不得标记通过。
- 本地浏览器已验证配置入口、二次验证到期禁用敏感操作、全局关闭时保留覆盖数量并禁止检索/补建，以及探测失败的脱敏提示。完整 Web 验收仍需覆盖：保存失败恢复、聊天切换丢弃旧响应、重复补建幂等、暂停与恢复、二次验证过期及收起敏感正文、执行来源失效。组件已有相应处理，实际浏览器演练结果需单独记录。

发布停止条件：真实召回率低于冻结门槛、无答案编造、跨聊天/事件边界泄漏、清理后仍可读取、重复出站消息、数据库升级演练失败，任一项未解决均不得合并发布。

### 真实 Embedding 首轮验证（2026-09-10）

使用管理员提供的局域网 Ollama 服务，模型 `qwen3-embedding:0.6b`（Q8_0，1024 维）。原生 `/api/embed` 与兼容 `/v1/embeddings` 均成功；兼容接口两条输入返回索引 0、1 及两个 1024 维向量。应用请求改为 `keep_alive: -1`，运行状态显示 Embedding 已加载。

仓库内 40 个虚构问题（10 个保留题，其中 8 个有答案）候选校准：开发集关键词/向量/混合 Recall@5 均为 100%；保留集分别为 87.5%、100%、87.5%。独立 PostgreSQL 测试库通过实际 Worker 和 MemorySession 验证同一批样本：20 条消息全部索引、失败缺口 0；开发集混合 Recall@5 为 100%，保留集为 87.5%，40 次查询 p95 为 1002 ms，20 条消息索引用时约 3.69 秒。时间范围使用显式 UTC 时间；日期字符串必须避免数据库会话时区歧义。以上小样本数据不能推算十万条历史补建耗时。

本轮本地 `pnpm verify` 309 项测试通过。此前提交 `aa3bc36` 的 CI 验证、旧 Alpine PostgreSQL 备份恢复、Compose 检查与镜像构建均已通过。

仍待执行：真实回答模型的摘要基线、无答案拒答、工具选择与引用回答评估，以及 Gemma 同时驻留并处理请求时的性能测试。首次远程 `/api/ps` 返回空列表，Embedding 请求后只报告 Embedding，未确认截图中的 Gemma 并行状态；不能据此宣称并行验收通过。保持草稿 PR，不合并 main，dev 镜像发布等待用户另行指令。

### 4B 对比试验（2026-09-10）

管理员已下载并授权测试 `qwen3-embedding:4b`，实际为 Q4_K_M、2560 维。先卸载 0.6B Embedding，再加载 4B；没有停止或重启 Gemma/Ollama。相同 40 题候选校准与独立数据库实际 Worker/MemorySession 测试均得到开发集混合 Recall@5 100%、保留集 87.5%；纯向量候选保留集 100%，与 0.6B 本轮样本结果相同。

20 条虚构消息全部索引，耗时 10.10 秒；40 次完整检索 p95 为 616 ms。与前次 0.6B 的 3.69 秒和 1002 ms 属于不同时间的单轮观测，未控制宿主机其他负载，不能断言 4B 检索更快。Ollama API 报告 4B 驻留约 4.09 GB（十进制），0.6B 前次为 2.15 GB。API 仍未报告 MLX Gemma 的驻留信息，双模型并行性能未验收。

4B 预热脚本为 [ollama_run_qwen3_embedding_4b.sh](../scripts/ollama_run_qwen3_embedding_4b.sh)，在 Mac mini 本机执行。脚本请求永久驻留，不自动卸载其他模型。试验后保留 4B 驻留，未修改生产应用的 Embedding 配置；启用 4B 必须使用 2560 维并创建新索引代次。本轮没有准确率收益证据，0.6B 仍可作为低内存基线。

### 补建进度验收

使用虚构消息验证固定范围、长短消息、清理移除、暂停恢复和旧租约拒绝发布；已处理与已移除之和不能超过总数。任务完成与聊天覆盖分开核对。ETA 验证至少三批/十五秒采样、十批窗口、无进度停估、失败与重启清空样本。Web 验证五秒单请求轮询、隐藏/卸载停止、过期响应丢弃、网络恢复及敏感授权过期。升级不会自动补建生产消息。

2026-09-10 本地补建进度验证记录：PostgreSQL 16 + pgvector 完成追加迁移及 316 项全量测试；`pnpm security:check` 无已知生产依赖漏洞，ShellCheck 通过。浏览器使用本地虚构 API 样本验证真实 Web 构建：无敏感授权时可读进度、操作按钮禁用，计数自动更新，任务与聊天覆盖分开展示。未启动生产补建，未发布镜像。

| 虚构消息数 | 固定范围元数据初始化 | 任务状态查询热 p95 | 前 10 批处理均值 |
| --- | --- | --- | --- |
| 10,000 | 176 ms | 6 ms | 21 ms |
| 100,000 | 1,508 ms | 17 ms | 49 ms |

上述批次测试使用虚构二维 Embedding 返回值，排除 Ollama 和真实模型延迟；每批最多读取 100 条，测试累计处理 1,000 条。首次实验发现每批扫描全部待处理成员会增加开销，改为按游标经过范围核对清理后复测；查询计划确认使用 pending 范围索引和消息主键。数值是本地观测，不是 NAS 吞吐或 ETA 精度承诺。任务成员表含主键与范围索引，十万成员约增加 22 MB 存储（数据库页分配存在波动）。

长期记忆界面一致性验收：检查侧栏定位与工作区滚动、每页 25 条任务列表、跨页详情、弹窗关闭与焦点恢复、敏感操作禁用、来源正文关闭/授权过期清空、分页失败回退及过期响应拒绝应用。PostgreSQL 用 125 个相同创建时刻的虚构任务验证分页无遗漏与重复，并验证聊天隔离和单任务读取。

2026-09-10 一致性修正验证：修复 AI 诊断 Fixture 的全聊天触发条件后，在独立 PostgreSQL 16 + pgvector 数据库完成 `pnpm verify`，317 项测试通过；安全检查、ShellCheck 通过。浏览器用虚构 API 验证分页、跨页任务详情、无敏感授权禁用操作，以及 Escape 关闭后返回原详情按钮。未执行生产操作；本机无 Docker，未重做 Compose 和镜像构建。

### AI 原始响应诊断

本轮使用虚构材料验证原始响应正文、上游 error.message/code/type/param 和白名单请求 ID。覆盖相同请求多次调用的响应隔离、HTML/空响应、UTF-8 64 KiB 截断、缓存淘汰后迟到响应不复活、未认证和错误 Attempt 拒绝，以及普通执行详情不包含正文。PostgreSQL 16 + pgvector 下完整验证通过。浏览器验收采用本地虚构 503 响应，确认执行弹窗的 AI 请求详情独占整行，错误字段、响应正文和原始请求按组展开；正文按纯文本显示并提供复制。响应正文和提取字段仅在敏感授权后读取，关闭弹窗或授权到期时清除，迟到响应不得重新显示。

iMessage 输出格式回归覆盖文本聊天与关闭上下文的润色节点均注入格式提示、保留自定义角色提示、JSON 节点不注入，以及 HTML 标签、泛型、比较符号、Markdown 风格运算符、缩进和空行在标记代码中的保留。自动测试验证提示组装与发送端行为，不将其等同于模型遵循率验收；真实模型冒烟需另用虚构日常问题与代码问题验证。


## 近期发言与上下文缺口回归

确定性测试覆盖：最新发言不受旧话题相关度影响、同时间索引排序、跨日偏移时间、延迟归档、发言人过滤、聊天与触发边界、清理、撤权、重复查询、零结果、证据截断及上下文时间极值。数据库场景位于 PostgreSQL memory 集成测试，需设置 `TEST_DATABASE_URL` 并先迁移和准备 pgvector；没有数据库时跳过不能作为通过证据。

模型行为另行验收，使用虚构成员 alice@example.test 和 bob@example.test：

| 输入场景 | 预期工具行为 |
| --- | --- |
| Alice 最后一次说话是什么时候 | 最近发言工具，准确 senderId |
| 今天零点到一点 Alice 说了什么 | 最近发言工具，当前消息日期、聊天时区及时间范围 |
| 最近一次讨论备份是什么时候 | 主题搜索，不能仅凭相关度宣称最后一次 |
| 两位成员同名，问小林最后说了什么 | 不猜 senderId，澄清身份 |
| 存在裁剪空档，问昨天是否没人说话 | 不把缺失上下文当作无人发言证据 |

真实模型验收需记录模型、版本、工具选择与实际答案；模拟 Provider 的自动测试不能替代该记录。执行详情人工检查：保留及裁剪范围与模型覆盖元数据一致；无裁剪显示“无”；旧快照显示“未记录覆盖范围”。本轮未接入可用模型时将该项记录为未执行。

上下文不完整及裁剪说明按普通辅助文字呈现，不使用红色警告文字或警告背景；仅调整视觉样式，不改变上下文覆盖数据、裁剪策略或执行状态。

## Agent 统一预算验收（2026-09-14）

本轮使用虚构消息和独立本地 PostgreSQL 16 + pgvector 数据库，应用全部迁移至 0048；没有读取 NAS 归档、修改生产配置或发布镜像。

| 验证层 | 场景与证据 |
| --- | --- |
| 确定性预算测试 | 混合批次共享计额、重复与缓存命中、参数错误、调用超额逐项回包、序列化字符累计、完整来源裁剪、控制消息不计正文预算、超时取消及收尾 |
| Agent 集成 | 默认允许超过旧的分类调用限制、配置运行中不切换、下一次运行读新版本、N＋2 轮预留引用修正、强制联网失败、关闭工具后继续调用失败 |
| PostgreSQL | 0048 新表、默认回退、并发保存冲突和数据库范围约束；授权撤销、重复来源归一、截断只保存已返回来源、取消后迟到结果不登记来源 |
| 工作流与权限 | 两个 AI 节点分别记录运行快照、失败节点保留预算摘要、管理员读写与更新审计、未登录拒绝、旧字段可选 |
| 浏览器操作 | 在本地组件 Fixture 页面验证默认加载、保存与版本更新、恢复默认但未保存、重新加载旧值、超范围输入阻止保存；预算耗尽与“未记录 Agent 预算”按普通提示渲染 |

真实模型行为评测与上述确定性测试分开：已提供 `pnpm agent:evaluate` 的五个固定虚构场景及新旧基线对比，尚未调用真实模型。按用户安排，待 NAS 环境发布后协同验证；本轮不声称新默认配额已被证明提高模型准确率。NAS 验收需记录模型/路由版本、每个场景的完成情况、工具序列、等待时间、Token 与人工判定，并核对页面快照与实际模型输入一致。

本地验证结果：

- `TEST_DATABASE_URL=<独立虚构测试库> pnpm verify`：通过，57 个测试文件、348 项测试全部通过，无跳过；包含 PostgreSQL 仓储与取消事务回滚测试，前后端构建通过。
- `pnpm security:check`：通过，未发现已知依赖漏洞。
- `docker compose --env-file .env.example config --quiet`：通过，使用示例配置；校验不启动容器，也不读取 NAS 配置。
- `pnpm agent:evaluate`：未配置真实模型端点时按合同返回退出码 2；这不是模型评测通过，真实模型结果待 NAS 协同验证。

额外验证了来源插入事务正在等待时取消调用：连接退出后事务回滚，`memory_retrieval_sources` 中没有该次迟到来源，运行会话也未登记引用。检索编码沿用 Embedding 客户端 15 秒单请求保护，同时服从 Agent 剩余时间；不再保留原来的 5 秒累计历史查询配额。

## 五工具本地验收（2026-09-14）

迁移 0049 已在独立 PostgreSQL 16 + pgvector 虚构测试库执行。没有使用真实归档、调用生产模型或发布 NAS。

| 确定性验证 | 已覆盖内容 |
| --- | --- |
| 工具合同 | 仅注册五个工具；默认值、严格参数、身份/时区/聊天上界不可覆盖、每日时段和 366 日上限 |
| 原始 SQL | 关键词 all/any、大小写、字面 %/_、消息计数、成员筛选、包含时间端点、微秒 keyset、延迟归档、其他聊天与触发后边界 |
| 分组 | 聚合后分页、空日期、NULL 成员独立分组、跨午夜与夏令时重复小时、每天首末消息 |
| 会话 | 裁剪后游标不跳过记录、分页分组不丢日期、跨会话/筛选变更/伪造游标拒绝、重复来源复用、首末引用展开 |
| 失败与预算 | 关闭向量服务仍可查询、权限撤销、正文清理、查询失败不伪造零、剩余时长取消、迟到来源隔离；现有统一预算和强制联网测试通过 |

本地完整验证：58 个测试文件、356 项测试全部通过，无跳过；pnpm verify 的格式、Lint、类型、PostgreSQL 集成、合同、文档链接及前后端构建均通过。pnpm security:check 未发现已知漏洞；docker compose --env-file .env.example config --quiet 通过。

可重放的 pnpm chat:benchmark 生成 TEMP 虚构表，调用实际 ChatQueryRepository，每组预热 1 次、测量 10 次；本次结果如下：

| 消息数 | 查询 | p50 | p95 |
| --- | --- | --- | --- |
| 10,000 | 成员最新消息 | 1 ms | 1 ms |
| 10,000 | 字面关键词精确计数 | 3 ms | 4 ms |
| 10,000 | 按天首条消息 | 7 ms | 8 ms |
| 100,000 | 成员最新消息 | 1 ms | 1 ms |
| 100,000 | 字面关键词精确计数 | 26 ms | 26 ms |
| 100,000 | 按天首条消息 | 96 ms | 99 ms |

EXPLAIN ANALYZE 显示成员最新查询使用部分 B-tree 索引反向扫描。此为本机热数据测试，不能推算 NAS、冷缓存、持续写入或长正文的表现。字面计数仍扫描匹配范围，超时返回 unavailable。无需因为新增两个工具提高默认共享预算；一天一调用的首末查询可合并为一次按天分组调用，实际模型效果待测。

真实模型评测仍未运行。pnpm agent:evaluate 现在包含 9 个固定场景：问候、最新发言、每天最早、保留已确认范围的原文追问、字面计数与原文组合、身份歧义、联网与历史组合、七天逐日调用、超额逐日调用。旧预算与新预算使用相同的当前工具集合和虚构响应，记录调用数、轮数、耗时、模型用量及人工评判；不能视为真实 SQL 或真实模型通过。按用户安排，待 NAS 开发环境发布后共同验证工具选择、回答与预算说明。


### NAS 对话核对与追问范围（2026-09-14）

对已部署开发镜像的两次人工对话，核对受保护的原始请求、工具结果和执行快照：按日首条查询与原文展开均成功；分别为 2 次工具 / 2 轮模型、9 次工具 / 3 轮模型，工具耗时约 0.08 / 0.60 秒，返回字符 3,118 / 14,420，正常完成。主 Provider 的 503 由现有 fallback 成功接替，不是工具失败。

发现连续追问时模型将已确定的七个日期扩大到八个日期；数据库按实际参数正确返回，属于模型重新解释范围。已在 Agent 协议补充沿用既有筛选范围的规则，并增加仅含虚构身份和正文的固定追问评测，输出工具参数供人工核对。该新增场景尚未运行真实模型；既有两次对话不是该提示修复的效果验证，也不代表完整模型评测通过。本轮不增加程序化事实审核，不保证完全消除模型日期错误。


## Bot 身份与归属验收

使用虚构工作流、成员和消息验证精确归属、两种到达顺序、缺失 ID、冲突、首次昵称回填、改名与执行快照、正文清理、角色过滤、当前与其他角色的 API 消息角色、身份失效和手动重建。原有权限、触发边界、配额和发送幂等继续回归。

`pnpm bot:evaluate` 使用固定虚构同名角色、改名、他人承诺与未知来源经历场景；使用 AGENT_EVAL_URL、AGENT_EVAL_MODEL 和可选环境凭据 AGENT_EVAL_SECRET，输出答案、模型用量、耗时和待人工判定标记。未配置端点返回 2，不算模型评测通过，不访问数据库或发送网关。确定性测试不证明模型永远不会混淆角色。真实模型评测按 NAS 协作安排单独记录。

本地验证记录（2026-09-14）：使用独立 PostgreSQL 16 + pgvector 虚构数据库，通过正式迁移入口执行 0001–0050 后运行 `pnpm verify`，61 个文件、366 项测试全部通过，无跳过；格式、Lint、类型、运维、104 个路由合同、文档链接和前后端构建均通过。`pnpm security:check` 未发现已知漏洞；`docker compose --env-file .env.example config --quiet` 通过。Web 构建保留既有超过 500 kB 的资源体积提示。

另在独立虚构数据库先执行 0001–0049、写入旧摘要，再执行 0050，验证旧摘要隔离及归属诊断字段。新增回归确认迟到重复发送记录会撤销已知角色、标记冲突并使派生数据失效；未知来源的诊断变化不改变证据哈希。重建测试设置待归属屏障并清理自身任务，避免全局摘要队列干扰其他测试。

本地浏览器已验证工作流昵称保存、版本反馈和独立折叠的历史维护入口；没有修改真实工作流。真实模型评测未运行，缺少端点时评测入口按约定退出 2。没有发布镜像、修改 NAS 或触发真实模型重建。

### 角色归属弹窗与重建导航（2026-09-14）

工作流右上角打开角色弹窗；只显示本自动化，表格按精确发送记录展示可归属、已绑定、待绑定，首次昵称补充单独计数。新增 PostgreSQL 虚构场景覆盖未绑定到已绑定、待补昵称、未知消息及冲突不计入某工作流；索引单项重建不会重置摘要。API 验证管理员权限、非法 target 拒绝、单项选择、无请求体兼容 both 及审计。

本地浏览器验证弹窗布局、Esc 关闭与焦点返回，以及“重建摘要”“重建索引”分别定位 AI 服务对应操作区；顶部导航不会遮挡目标标题。验证没有触发真实模型调用或修改 NAS。最终 pnpm verify 在独立 PostgreSQL 16 + pgvector 虚构库运行，61 个文件、368 项测试全部通过；pnpm security:check 无已知漏洞，Compose 配置校验通过。保留既有 Web 构建资源超过 500 kB 提示。

角色与聊天维护验收：自动化列表显示角色昵称及未配置状态；角色弹窗只显示本工作流信息并引导前往聊天管理。聊天列表仅为待重建项目显示对应按钮；停用聊天不可重建，请求处理中阻止重复提交，启动后刷新状态并提示审计入口。待重建或已提交重建的摘要不提供普通重置按钮。无待重建标记的聊天不显示重建按钮。

角色重建进度回归使用虚构消息，通过实际角色重建入口创建无 request_key 的任务，验证完整成员计数、分批处理、预估生成、失败重试与暂停恢复保留重建类型；另验证旧无请求键补建任务仅初始化剩余范围并清空无效采样。不修改 NAS 上正在运行的任务。

### 摘要异常保护与失败批次恢复（2026-09-14，历史记录，已由迁移 0052 取代）

使用独立本地 PostgreSQL 数据库与虚构消息验证：取消目标字数、12,000 字符异常保护及 8,192 输出 token 参数；超长时使用同一来源最多追加一次紧凑重试；两次失败保留已提交摘要和游标；并发手动重试只重新入队一次，保留成功进度与尝试历史，服务停用时不恢复任务。历史重新生成预览仍不修改正式摘要。

`pnpm verify` 通过（61 个测试文件、377 项测试，含 PostgreSQL 集成测试）；`pnpm security:check` 无已知漏洞；`docker compose --env-file .env.example config --quiet` 通过。代码和 Web 生产构建通过。真实模型的摘要长度、推理 token 消耗及 NAS 界面联调待后续部署后验证，本次未调用真实模型验证摘要效果，不将放宽配额等同于质量提升。


## 聊天 AI 摘要退休与原文双窗口（迁移 0052）

确定性测试使用虚构数据：窗口阈值与批量推进、重复触发与并发、重启复用、配置变更、触发边界及跨聊天拒绝、延迟发送时间、本账号过滤、正文清理、单条超长、字符裁剪、原文前缀与图片归属、旧输出引用错误、配置权限／审计／并发保存。执行快照与模型输入共享覆盖信息，普通提示不使用警告色。

迁移测试新建独立临时数据库，应用旧迁移并插入旧窗口设置、摘要状态／版本／任务、执行与节点快照、原文、图片摘要和 active 向量代际，再运行 0052。验证旧设置原值迁移、摘要清除、图片／链接字段及原文保留、向量代际仍 active，以及身份触发器在旧表删除后继续有效。测试结束删除临时库。备份检查脚本兼容迁移前后的数据库结构。

本地 `pnpm verify`：62 个测试文件、359 项测试通过，包含 PostgreSQL 集成与迁移测试；服务端、Vue 类型检查与生产构建通过。`pnpm security:check` 未发现已知漏洞，`docker compose --env-file .env.example config --quiet` 通过。构建仍有现存的大体积前端 bundle 提示。

模型行为评测：`pnpm agent:evaluate` 增加固定的缺口查询与可见原文直接回答场景；本轮未配置开发端点，真实模型调用选择、答案质量和 Provider 缓存命中待运行，不计入确定性测试通过数量。

界面验证：已运行前端类型检查和生产构建，并通过 Chrome 打开本地虚构数据页面 `http://127.0.0.1:4198/#/ai`，验证默认配置加载、修改基础窗口后保存并更新版本、恢复默认但未保存时刷新仍保留旧值，以及恢复默认并保存后更新版本。此前使用错误的 `/ai` 路径（返回 404），一次浏览器工具错误不足以证明本地页面被禁止访问；正确的 hash 路由可正常打开。该 Fixture 未接入 MemoryService，其不可用提示不代表生产服务故障。执行详情的浏览器交互验收及真实模型联调仍待完成；NAS 未更新，待用户安排部署后联调。

### 生成保护与历史展开回归（2026-09-15）

确定性测试使用虚构数据：验证新旧配置互斥与默认值、提示目标与三倍硬保护、Chat Completions length 和 Responses incomplete（空正文、非空正文、工具参数未完成）、缺少结束原因、有限重试与备用路由且不降级 Provider；检索默认 8/可选 20、实际行展开、清理与索引缺口、触发上界、来源保留及内容预算。原有 JSON、引用修正、图片摘要与授权测试继续执行。

模型行为评测单独记录，不把单元测试视为真实模型通过。固定虚构场景：20 天中文事件时间线（检查完整结尾和日期）、三轮历史查询与更正（检查先检索再回答、证据足够即停止）、含虚构会议日期的图片（检查文字提取和 500 字符目标）。比较旧聊天 1024 Token/4000 字符与新聊天 8192 Token/2000 目标/6000 保护；图片比较旧 384 Token 与新 2048 Token。记录完成/失败、结束原因、输出字符、Token、耗时、工具调用、人工支持性判断。无开发模型端点时保持待运行，不自动使用 NAS 凭据；更大预算不代表更高准确率。

本地验证结果：独立虚构 PostgreSQL 16 + pgvector 数据库完成全部迁移；pnpm verify 全部通过（63 个测试文件、373 个测试，无跳过），含服务端/Web 构建、合同、链接及运维检查。pnpm security:check 未发现已知漏洞；docker compose --env-file .env.example config --quiet 通过。本地 Chrome 挂载实际 NodeInspector 组件验证旧配置保持、主动升级删除旧字段、2000→6000 与 3000→9000 联动。临时验证页已移除。pnpm agent:evaluate 因未提供开发模型端点以退出码 2 明确标记未运行；上述新旧生成预算及图片场景需在后续 NAS 联调补记真实结果。未发布镜像、未修改 NAS 或激活工作流。


### 页面模块导航

AI 服务、聊天与归档、执行与审计、系统设置共用滚动导航。导航及键盘焦点顺序按页面模块实际位置排列，不依赖配置数组顺序；滚动边界裁剪前先确定模块顺序。选中背景根据相邻模块起点之间的实际滚动进度连续移动；到达页底时对齐末尾模块。点击模块平滑滚动，减少动态效果偏好下直接定位。异步内容改变高度和窗口尺寸变化后重新计算位置；窄屏横向排列时同步计算横向位置。键盘焦点与当前模块标记独立保留。

验收覆盖长短模块、页首/页底、点击后手动反向滚动、异步内容展开与收起、窄屏横向导航和路由切换后的监听清理。

### 未知发送告警与处理定位

- 首页未知发送卡片携带 `attention=unknown-outbound`，进入执行记录后自动定位筛选列表，打开详情定位出站发送；恢复队列入口仅展示未关闭的 retrying、failed、dead-lettered 执行。可切回全部历史。
- 人工关闭保留原始 unknown 及审计，但移出待处理统计；不开放重发，不把未知状态改成成功。已关闭历史无需回填。
- 2026-09-17 本地 `pnpm verify` 通过（317 项通过，58 项数据库测试未在该命令中运行）；`pnpm security:check` 通过。另启动隔离的 PostgreSQL 16 虚构测试库，执行全部迁移及 `tests/postgres-workflow-repository.integration.test.ts`，2 项通过，包含真实 SQL 的筛选、关闭前后计数和 unknown 保留断言。
- 本地虚构页面确认筛选说明、详情出站信息、人工重试禁用和关闭确认文案。浏览器自动化在原生确认框处受阻，关闭后的 UI 刷新未完成浏览器验证；相应服务端行为已通过 API 和 SQL 测试。本机无 Docker，未执行 Compose 校验；本轮未修改部署配置。


### 引用格式兜底回归

使用虚构查询分别覆盖仅网页、仅历史及混合工具返回后的无效引用：最多纠正一次，后续无效标记清理不得替换回答语义；纠正成功、空文本、无效 JSON、嵌套 JSON、预算末轮不重新开放工具均有确定性回归。历史真实来源的渲染、授权与清理继续由 PostgreSQL 集成测试覆盖。真实模型行为验证尚待开发环境复测，不将确定性测试等同于模型准确率评测。

### 恢复队列批量关闭回归

- 使用虚构数据验证超过一页的待恢复记录全部关闭，运行中、排队中和成功记录不变；重复操作返回零条。
- 验证 unknown 出站轨迹保留且不重发；关闭等待重试任务后定时恢复不再发送。
- 未登录返回 401，未解锁敏感操作返回 403，拒绝与成功均记录 `execution.bulk-close` 审计。
- 界面仅在恢复队列显示入口，未解锁、空列表或请求中禁用；确认涵盖全部分页，完成显示实际数量，列表刷新失败独立提示。


### 表情库迭代验证

确定性测试使用程序生成的虚构图片及虚构聊天。入口为 `tests/meme-*.test.ts` 与 `tests/postgres-memes.integration.test.ts`；验证图片解码、拒绝伪装格式/损坏/超限、哈希去重、授权和审计、候选限定、参数/预算/迟到结果、人工摘要冲突、独立图片失败/未知处置及原文件 multipart。全量回归通过 TEST_DATABASE_URL 指向独立测试库执行，不复用生产归档。

真实模型行为评测与单元测试分开记录，待开发模型端点或NAS联调：固定虚构场景包括普通问候（不必检索）、庆祝完成（可选开心表情）、严肃故障说明（优先文字）、无匹配素材（只发文字）、检索后预算耗尽（完整文字收尾）、素材选择后被停用（不换图）。记录工具序列、选择比例、回答完整性和用量，不以命中关键词代替人工质量评价。

浏览器验收检查上传进度、生成中/失败/候选、保存冲突、取消删除、防重复提交、分页刷新失败和 GIF 按需播放。部署后使用已授权聊天与素材分别核对文字先于图片、接收端GIF动画、独立状态、图片单独重试及未知关闭；不得将HTTP200等同于客户端全部兼容。新增素材卷备份恢复演练需在独立环境复制数据库及素材，验证停机一致性与文件哈希。

2026-09-18 本地验证证据：独立 PostgreSQL 16 测试实例完成0053及全部前序迁移、pgvector准备；`pnpm verify` 完成402项测试（71个测试文件，包含全部PostgreSQL集成测试）及Web/服务端构建。`pnpm security:check` 无已知漏洞。本地浏览器验证登录但未敏感解锁时上传虚构GIF、静态卡片、原文件预览及人工摘要保存。配套备份使用 pg_dump/pg_restore 和素材目录副本，恢复后核对两张虚构素材的原文件SHA-256与预览可读性。

Docker CLI 在本机不可用，`docker compose --env-file .env.example config --quiet` 返回 command not found，不能记为通过；Compose素材卷挂载及容器UID权限仍需镜像构建环境验证。真实模型选图适度性、真实接收客户端动画兼容性尚未重跑，未自动向NAS或聊天发送验证请求。

### 表情检索与上传体验优化

确定性回归覆盖空关键词浏览的启停/删除过滤、稳定顺序与条数限制、浏览候选选择、字面查询不自动分词。摘要请求携带名称、描述、标签，提示只输出素材摘要；输出质量属于模型评测，不以提示词断言替代真实评测。上传区检查选图/更换、预览、格式/大小提示、拖放反馈、进度及防重复提交。

模型场景：用虚构“小狗跳舞”（摘要含“卖萌”）分别测试“随便来个表情”和“吃饭出发”，观察模型是否使用空关键词浏览、是否将空结果误报为故障或承诺自动补发。真实模型评测待后续 NAS 验证，本轮不自动发布或发送消息。

本地验证（2026-09-18）：`pnpm verify` 通过，71个测试文件、404项测试全部通过，包含 PostgreSQL16 集成；`pnpm security:check` 无已知漏洞。浏览器使用虚构 GIF 验证选图控件、预览、文件大小、空文件禁用提交及上传后关闭弹窗。Docker CLI 在本机不可用，Compose 本轮未改动，配置校验需发布CI执行。

### 表情编辑布局与静默刷新

编辑弹窗采用预览/资料双栏，窄屏改为单栏；GIF播放、启用开关、摘要状态、候选采用和删除入口使用统一控件。列表每5秒后台刷新不插入加载行，手动查询使用固定宽度按钮显示忙碌状态，保留现有卡片。验收时检查刷新前后卡片位置、候选长文本滚动、窄屏布局和禁用状态。

2026-09-18本地验证：虚构素材浏览器检查双栏预览、开关及摘要候选区域；`pnpm verify`通过（342项通过、62项PostgreSQL测试因未设置测试数据库跳过），`pnpm security:check`无已知漏洞。本次仅调整Web展示，未变更数据库或Compose。

### 表情合集与批量管理（0054）

2026-09-18 本地验证使用独立 PostgreSQL 16 测试库及虚构素材，无生产数据或 NAS 写入。`pnpm verify` 通过 74 个测试文件、414 项测试（无跳过），包含合集 PostgreSQL 集成测试；OpenAPI 校验 118 个运行时路由，文档链接与前后端构建通过。构建保留已有的大包体积提示。`pnpm security:check` 无已知漏洞。

本机没有 Docker CLI，使用 Docker 官方发布、校验 SHA-256 的独立 Compose v5.5.1 执行 `--env-file .env.example config --quiet`，配置校验通过；此项不代表已启动容器或验证 NAS 挂载，本轮 Compose 未变更。

| 验证范围 | 证据 |
| --- | --- |
| 合集与兼容 | `postgres-meme-collections.integration.test.ts` 覆盖名称冲突、版本冲突、封面回退、删除解除归属、去重不移动、旧客户端省略归属 |
| 固定选择与并发 | 424 张虚构素材冻结选择，之后新增不纳入；并发修改返回 1 项冲突，其余 423 项成功；5,001 张选择明确拒绝；删除与移动并发无悬空归属 |
| 摘要与检索 | 移动、启停不影响新任务摘要写入；人工输入变更与旧任务保守检查仍有效；合集名称参与召回与权重排序，停用过滤和工具预算回归通过 |
| 权限与未知结果 | `meme-auth.test.ts` 验证管理员权限、无需敏感解锁及审计；`meme-batch-client.test.ts` 验证丢失响应后的读取核对，不重放写请求，不将读取失败视作已删除 |
| 整理脚本 | `meme-organizer.test.ts` 验证预览无写入、哈希核对、人工归属保护、重复执行；只提供虚构清单样例 |
| 浏览器 | 真实 Vue 页面配合本地虚构 API：桌面合集侧栏、390 像素窄屏选择器、默认上传归属、新建与空合集、424 张跨页全选及批量停用成功；切换筛选清空选择并禁用旧列表选择，URL 查询刷新恢复 |

批量操作的停止只影响尚未提交批次，未知项须核对后才能再次批量提交。旧素材迁移后为未分类，“什么猫”真实清单整理及 NAS 验证留待部署后明确执行。此次不改变模型工具选择策略，未新增真实模型调用评测或外部聊天发送。

### 表情使用统计与排序（2026-09-18）

使用独立虚构 PostgreSQL 16 数据库验证迁移 0055；迁移测试在隔离 Schema 中准备旧表和确认记录，覆盖历史回填、缺少确认时间、标记防重、并发确认、失败后成功、事务回滚、关闭后确认不计次、删除投递不扣减及管理元数据不变。真实仓储测试覆盖五种排序、零次、空时间、次数同分、合集分页和冻结选择版本。发送服务测试检查文字成功不计次、失败/未知/关闭不计次、恢复成功一次计数和失效租约的迟到确认隔离。

本地浏览器使用 30 张虚构素材验证实际 MemesView：URL 恢复使用最多、选择当前页后切换使用最少会清空选择、零次详情显示尚未使用；检查桌面和 390px 手机布局。预览使用模拟接口，不替代 NAS 端到端发送验证。原有静默刷新和选择时暂停刷新逻辑保持不变。

验证命令：设置 TEST_DATABASE_URL 后 pnpm verify（含 PostgreSQL 集成测试），pnpm security:check，以及官方独立 Compose CLI 的 --env-file .env.example config --quiet。本轮不进行生产发送或部署。升级前已清理记录无法补算。

本轮结果：pnpm verify 通过（75 个测试文件、416 项测试，无跳过，含 PostgreSQL 集成）；pnpm security:check 未发现已知漏洞；Compose 配置校验通过。Web 构建仅保留现有大块体积提示。
